{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean",
      "enum": [true]
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object",
      "properties": {
        "icon": {
          "type": "string"
        },
        "environment": {
          "type": "string"
        }
      },
      "required": ["icon", "environment"]
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["success", "message", "data", "timestamp"]
}
