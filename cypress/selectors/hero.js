const body = 'body'
const wrapper = '[data-cy="wrapper"]'
const bg = '[data-cy="bg"]'
const main = '[data-cy="main"]'
const header = '[data-cy="header"]'
const headingText = '[data-cy="heading-text"]'
const paragraphText = '[data-cy="paragraph-text"]'

const heroSelectors = {
  body,
  wrapper,
  bg,
  main,
  header,
  headingText,
  paragraphText,
}

export default heroSelectors
